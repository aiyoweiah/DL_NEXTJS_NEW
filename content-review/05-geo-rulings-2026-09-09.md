# GEO audit rulings R1–R7 — staged cascade · ✅ A–E APPLIED 2026-09-09 · F/G deferred · H APPLIED (D109)

**Ruled 2026-09-09 (owner, in chat)** on the seven questions in [`../docs/geo-audit-2026-09.md`](../docs/geo-audit-2026-09.md) § Rulings requested. Logged as **D102–D105** in `docs/decision-log.md`; status rows in `decision-index.md`. **Nothing below is applied.** The owner reviews the Proposed column, adjusts wording freely, picks an option where one is asked for, then says **"apply"**. Line numbers are against `ed9f266`.

| R | Ruling | Result |
|---|---|---|
| R1 | Founding year | **2020** → D102, § A below |
| R2 | Credential wording | **degrees** → D103, § B |
| R3 | March blog bylines | **placeholders** → blog options evaluated, § F (pick B or C) |
| R4 | `/assessment` as a second contact page | evaluated, § G (recommend against; two alternatives) |
| R5 | City schema | **`areaServed` only** → D104, § C |
| R6 | YouTube channel is DODO's | **yes** → D105, § D |
| R7 | OG card | **placeholder confirmed** → § H, four candidates rendered |

---

## A · D102 — founding year 2020 (11 rows)

The `/about` numbers section already says it (`marketing.en.js:556` "Founded in 2020 in Canada. Relaunched 2025…"; `zh:531`). The founder bio three sections below, the Person schema and all three llms files said 2021. One year, everywhere:

| # | File:line | Live | Proposed |
|---|---|---|---|
| A1 | `content/marketing.en.js:1825` (`founder.short`) | "In 2021 she founded DODO Learning; since then more than 300 " | "In 2020 she founded DODO Learning; since then more than 300 " |
| A2 | `content/marketing.en.js:1841` (`founder.long`) | "In 2021 she founded DODO so that no student would " | "In 2020 she founded DODO so that no student would " |
| A3 | `content/marketing.en.js:1854` | `foundingYear: '2021',` | `foundingYear: '2020',` |
| A4 | `content/marketing.zh.js:1739` | "获商学学士学位。2021 年，她创立了 " | "获商学学士学位。2020 年，她创立了 " |
| A5 | `content/marketing.zh.js:1752` | "2021 年，她创办了 DODO，让后面的孩子…" | "2020 年，她创办了 DODO，让后面的孩子…" |
| A6 | `content/marketing.zh.js:1762` | `foundingYear: '2021',` | `foundingYear: '2020',` |
| A7 | `lib/schema.js:167` (`educationOrgSchema`) | `foundingDate: '2021',` | `foundingDate: '2020',` |
| A8 | `lib/schema.js:456` (`personSchema.description`) | "In 2021 she founded DODO Learning; since then more than 300 " | "In 2020 she founded DODO Learning; since then more than 300 " |
| A9 | `public/llms.txt:65` | "DODO Learning was founded in 2021 by **Janet**" | "DODO Learning was founded in 2020 by **Janet**" |
| A10 | `public/llms-full.txt:206` · `:210` | "founded in 2021 by **Janet**" · "In 2021 she founded DODO Learning." | "founded in 2020 by **Janet**" · "In 2020 she founded DODO Learning." |
| A11 | `public/llms-full.zh.txt:189` · `:193` | "于 2021 年创立。" · "2021 年，她创办了 DODO" | "于 2020 年创立。" · "2020 年，她创办了 DODO" |

**Flag A-i.** The bio's arc reads "graduated 2019 … founded 2020 … relaunched 2025". Nothing in the bio mentions the 2025 relaunch; the `/about` numbers block does. Leaving the bio silent on it is fine; adding it is new copy (not drafted).

## B · D103 — "degrees", never "graduate degrees" (13 rows)

§11 / D18: **Degrees from world top-50 universities**. `llms.txt:9` ("are graduates of world top-50 universities") already conforms and is the reference sentence.

| # | File:line | Live (fragment) | Proposed |
|---|---|---|---|
| B1 | `content/faq.js:125` (EN "academic backgrounds") | "Every Navigator holds a graduate degree from a world top-50 university (Oxford, U of T, Queen's, LSE and others)" | "Every Navigator holds a degree from a world top-50 university (Oxford, U of T, Queen's, LSE and others)" |
| B2 | `content/faq.js:252` (ZH) | "每位导师均持有世界排名前50大学（…）的研究生学位，" | "每位导师均持有世界排名前50大学（…）的学位，" |
| B3 | `content/marketing.en.js:572` (`/about`) | "Our ELA Program Navigators hold graduate degrees from world top-50 universities" | "Our ELA Program Navigators hold degrees from world top-50 universities" |
| B4 | `content/marketing.zh.js:547` (`/about`) | "都拥有世界排名前 50 的大学（…）的研究生学位，" | "都拥有世界排名前 50 的大学（…）的学位，" |
| B5 | `content/marketing.en.js:949` (`/program`) | "holding a graduate degree from a world top-50 university" | "holding a degree from a world top-50 university" |
| B6 | `content/marketing.zh.js:912` (`/program`) | "拥有世界排名前50的大学（…）的研究生学位，" | "拥有世界排名前50的大学（…）的学位，" |
| B7 | `content/marketing.en.js:1345–1346` (`/methodology`) | "by a Navigator with a graduate degree from a world top-50 university." | "by a Navigator with a degree from a world top-50 university." |
| B8 | `content/marketing.zh.js:1280` (`/methodology`) | "由拥有世界排名前50大学研究生学位的导师（Navigator）一对一交付。" | "由拥有世界排名前50大学学位的导师（Navigator）一对一交付。" |
| B9 | `content/marketing.en.js:1410` (`/navigators` "Credentialed") | "Every Navigator holds a graduate degree from a world top-50 university — Oxford, U of T, Queen's, LSE and others —" | "Every Navigator holds a degree from a world top-50 university — Oxford, U of T, Queen's, LSE and others —" |
| B10 | `content/marketing.zh.js:1344` (`/navigators` 资质) | "都拥有世界排名前50的大学（…）的研究生学位，" | "都拥有世界排名前50的大学（…）的学位，" |
| B11 | `public/llms-full.txt:9` | "a dedicated Navigator — a graduate-degree holder from a world top-50 university with a specialist background in English literature or composition —" | "a dedicated Navigator — a graduate of a world top-50 university with a specialist background in English literature or composition —" |
| B12 | `public/llms-full.txt:83` | "hold **graduate degrees** from world top-50 universities" | "hold **degrees** from world top-50 universities" |
| B13 | `public/llms-full.zh.txt:82` | "持有 **世界前 50 强大学的研究生学位**" | "持有 **世界前 50 强大学的学位**" |

**Flag B-i.** `content/zh/blog/lexile-asymmetry-bilingual-children.mdx:65` ("陈博士…应用语言学博士学位") is a placeholder bio and disappears with R3 — not rowed here.

## C · D104 — city schema: `areaServed` only

`lib/schema.js`, `citySchema()`:

| # | Line | Live | Proposed |
|---|---|---|---|
| C1 | `:622` (section comment) | `// LocalBusiness + EducationalOrganization composite.` | `// EducationalOrganization scoped by areaServed. D104 (2026-09-09): no LocalBusiness type and no PostalAddress — the program has no premises in any city.` |
| C2 | `:639` | `'@type':    ['EducationalOrganization', 'LocalBusiness'],` | `'@type':    'EducationalOrganization',` |
| C3 | `:649–654` | the `address: { '@type': 'PostalAddress', addressLocality: name, addressRegion: region, addressCountry: countryCode }` block | **delete** |

`areaServed`, `parentOrganization`, `contactPoint` stay. `countryCode` remains in the signature (unused after C3) — drop it from the JSDoc or leave; no caller breaks. **S1's `grades 3–8` on `:646` is a separate row (audit S1), not folded in here.**

## D · D105 — `sameAs` + footer link (YouTube)

| # | File:line | Live | Proposed |
|---|---|---|---|
| D1 | `lib/schema.js:183–187` (`educationOrgSchema`) | `// Same-as — add social profiles when confirmed` + commented array | `// sameAs — D105 (2026-09-09): YouTube confirmed by the owner. XHS / WeChat OA pending (workflow Open Decision #9).` newline `sameAs: ['https://www.youtube.com/@DODO-Learning'],` |
| D2 | `content/marketing.en.js:150` area (footer `resources`, after "Watch a Class") | — | `{ href: 'https://www.youtube.com/@DODO-Learning', label: 'YouTube', external: true },` |
| D3 | `content/marketing.zh.js:132` area (footer `resources`, after 课堂实录) | — | `{ href: 'https://www.youtube.com/@DODO-Learning', label: 'YouTube', external: true },` |

`Footer.jsx` already renders `external` links as `<a target="_blank" rel="noopener noreferrer">` (`:79–84`). `personSchema.sameAs` stays unset — no personal profile was ruled. **Flag D-i:** if the channel should also appear on the `/demos` page (where the class recordings will live), that is a page-copy change, not drafted.

## E · M1 — blog canonical (only if the blog survives, see § F)

| # | File:line | Live | Proposed |
|---|---|---|---|
| E1 | `app/[locale]/blog/[slug]/page.jsx:34` | `return buildPostMetadata(post.frontmatter, locale)` | `return buildPostMetadata({ ...post.frontmatter, slug: post.slug }, locale)` |

Under option C the route is removed and E1 is moot.

---

## F · R3 — the blog: retire it, or prune it to the one real post

**What is there.** Three EN posts, two ZH. The two March posts (`lexile-asymmetry-bilingual-children`, `what-does-lexile-score-mean`) carry placeholder bylines (owner-confirmed) **and** off-brand register: "intervention/intervene" ×12 and "ESL support" framing in EN, 干预 ×12 in ZH — §10 strip-on-sight, §07a remediation frame. The index (`app/[locale]/blog/page.jsx`) is a scaffold: hard-coded `featuredArticle` + `ARTICLES` (5 of 7 links 404), "Navigator picks" by "SC", 31 Unsplash hotlinks, "500+ diagnostic calls". The one real post — **MCT Language Arts in a live one-on-one program** (Janet, 2026-08-26, on-brand, the tracker's named unblock for A6/A8) — is not linked from the index, and its canonical points at `/blog/undefined/` (M1). No blog URL surfaced in the Bing-backed or WebSearch samples taken 2026-09-09; `/en/lexile/` did. The 2-week cadence planned in May never started.

**Impact of removal, by surface:**

| Surface | Retire whole blog (A) | Prune to the MCT post (B) | Retire `/blog`, re-home the MCT article as an evergreen page (C) |
|---|---|---|---|
| Search index | ≈ nothing to lose (no blog URL observed indexed; canonicals were broken); add 301s anyway | same | same |
| Sitemap | 38 → 34 URLs | 38 → 36 | 38 → 35 (new article URL) |
| Tracker A6 / A8 (MCT, Kumon-alternative) | **loses the only long-form MCT asset** | keeps it, now linked + canonical fixed | keeps it at a flat, descriptive URL (`/mct-language-arts/`) next to `/credentials` |
| E-E-A-T / trust | placeholder bylines + banned register gone | same, but a one-post "blog" | same, and no blog shell to read as abandoned |
| Freshness signal | none | a blog dated March/August looks dormant | an evergreen page carries `dateModified`, no cadence implied |
| Internal linking | Blog nav + footer links removed | index links the post; `/credentials` should link it too | `/credentials` + `/methodology` link the article; nav/footer "Blog" removed |
| `llms.txt` | nothing to add | add the post (F11) | add the article (F11) |
| Maintenance | zero | a route + pipeline kept for one post | one page; the blog can return when a cadence is real |

**Recommendation: C.** It keeps the one asset the GEO plan depends on, removes everything fabricated, and does not leave a "Blog" that visibly stopped. B is right only if the two-weekly cadence starts within a month; A gives up the MCT asset for nothing.

**Rows for C** (owner adjusts labels; new-copy items flagged):

| # | Change | Note |
|---|---|---|
| F-C1 | New route `app/[locale]/mct-language-arts/page.jsx` rendering the existing MDX (moved to `content/en/articles/mct-language-arts.mdx`) through the existing `mdxToHtml`; `buildMetadata({ ogType: 'article', publishedAt, updatedAt, authors: ['Janet'] })`; `articleSchema` gains an optional `basePath` (it hardcodes `/blog/`) | no copy authored |
| F-C2 | ZH route renders the EN article with the existing `fallbackNotice` ("本文暂无中文版本，以下为英文原文。"), canonical → the EN URL, `inLanguage: 'en'` (audit S5) — until a ZH translation exists (first job for the new ZH tooling after F9) | existing copy |
| F-C3 | `public/_redirects`: `/en/blog/mct-language-arts-in-a-live-one-on-one-program/ /en/mct-language-arts/ 301` (+ZH) · `/en/blog/lexile-asymmetry-bilingual-children/ /en/lexile/ 301` (+ZH) · `/en/blog/what-does-lexile-score-mean/ /en/lexile/ 301` (+ZH) · `/en/blog/ /en/credentials/ 301` (+ZH) · the bare `/blog/` and `/blog/:slug/` 302 rules → 301 to the same targets | — |
| F-C4 | Delete `app/[locale]/blog/`, `components/blog/BlogClient.jsx`, the four placeholder MDX files, `lib/blog.js` (keep `mdxToHtml` in a small `lib/mdx.js`); `app/sitemap.js`: drop `/blog` + `getBlogSlugs()`, add `/mct-language-arts` (priority 0.7, `lastModified` = the article's date); `PreCtaBand` suppress list `'/blog'` → `'/mct-language-arts'` | — |
| F-C5 | Nav: remove `{ href: '/blog', label: 'Blog' }` (`marketing.en.js:76`) and `博客` (`zh:67`). Footer Resources: remove the Blog rows (`en:150`, `zh:132`) | copy removal only |
| F-C6 | `/credentials` MCT card and `/methodology` research block gain a link to the article using its existing title | link only — **flag F-i:** any link label other than the title is new copy |
| F-C7 | `public/llms.txt` § Core pages: add the article line (title, date, author) — audit F11 | machine surface |
| F-C8 | `docs/workflow.md` Tier 3: mark the "1 pillar post every 2 weeks" line **suspended until the blog returns**; `docs/README.md`/handoff mention | docs |

**Rows for B** (if picked instead): delete the two placeholder posts + ZH twins with the same 301s to `/lexile/`; rebuild `blog/page.jsx` from `getAllPosts(locale)` (drop `featuredArticle`, `ARTICLES`, `CATEGORIES`, `BlogClient`, the "Navigator picks" block and all Unsplash `img`s; keep the `geoLinks` block); apply E1; apply F-C2's fallback canonical rule to `blog/[slug]`; sitemap `changeFrequency` for `/blog` → `monthly`.

## G · R4 — `/assessment` as a second contact page

**What is there.** `app/[locale]/assessment/page.jsx` renders `UnderConstruction` (an animated placeholder with its own mini-nav). Linked only from the footer Program column as "The Lexile Assessment" / "Lexile 测评" with `soon: true`. Indexable, not in the sitemap. Meta description: "Measure your child's reading level with a Lexile baseline assessment."

| | For repurposing as a contact page | Against |
|---|---|---|
| URL | reuses an existing footer-linked URL | `/assessment` would name a page that is not an assessment; humans and models read URLs as content claims |
| Funnel | a lighter path for WeChat/email-first parents than the 20-minute diagnostic form | D27/D29/D30: `/consult` is the firm close, `/demos` the soft close, one action one label; a third contact entry splits the funnel and duplicates the schema `contactPoint` URL |
| D29 | closes the open question of an assessment page | it *is* the D29 violation in URL form — an assessment-named page that captures leads |
| Existing surfaces | — | `/consult` already carries `emailCard` and `wechatCard` (`marketing.en.js:728`, `:734`); the lighter path exists |
| Index | removes an under-construction page from the indexable set | so does `noIndex: true`, in one line |
| GEO | none | an explainer at this URL answers a tracked prompt (A7: "school gave a Lexile score, what next") and complements `/lexile` |

**Recommendation: do not repurpose.** Two alternatives, either of which can be picked now:

- **G-1 (preferred, when there is copy budget):** build the page the footer already promises — *How the DODO Lexile baseline assessment works*: what a baseline measures, MetaMetrics-certified administration, Week 0 · 8 · 16 cadence, what the family receives within 72 hours, how it feeds Navigator matching; informational, no assessment CTA, closes with the consult link. D29-compliant, both locales, ~600 words. **New copy → its own apply.**
- **G-2 (now, no copy):** `noIndex: true` on the placeholder and drop the `soon` footer rows (`en:146`, `zh:128`) until G-1 exists. If a lighter contact touch is wanted, surface the existing email/WeChat cards higher on `/consult` and add `email` to the Organization `contactPoint` (audit O4).

## H · R7 — the OG card is a placeholder: confirmed, and four candidates

**Confirmed.** `public/og-default.png` is 1200 × 630 with one flat colour (`#0E0E12`, luminance 14 at every sampled pixel), committed 2026-03-20 in `83830ea` ("0320exp"). No brief exists for it anywhere in `.design/`, `docs/` or the guides; `workflow.md:241` lists "provide per-page OG images (or approve text-based fallbacks)" as a user-side Tier-2 item. It ships as `og:image` / `twitter:image` on all 120 routes, as `Organization.logo`, and as the fallback `Article.image`.

**Candidates** — rendered from the built site with its own assets (`logo.svg` / `logo-dark.svg`, LXGW WenKai GB subset from `public/fonts/cjk/`, Source Sans 3), 1200 × 630, in [`../.design/og-card-2026-09/`](../.design/og-card-2026-09/): `og-en-light.png`, `og-zh-light.png`, `og-en-dark.png`, `og-zh-dark.png`. Copy on the card is limited to locked strings: the wordmark, the §16 first-mention pair "DODO Learning · 都学书院" as an eyebrow with the lead-in quote (D54), the D36 tagline in each locale, and the domain. **Recommendation: the light pair** (Whisper ground matches the site; chat apps render cards on both white and dark UIs). Nothing was hand-tuned by eye beyond one look; the owner's eye rules.

**Wiring rows (after the pick):**

| # | File:line | Live | Proposed |
|---|---|---|---|
| H1 | `public/og-default.png` · new `public/og-zh.png` | blank rectangle | overwrite `og-default.png` with the chosen EN card (keeps every cached external reference valid); add the ZH card |
| H2 | `lib/metadata.js:50–55` (`OG_IMAGE_DEFAULT`) + `buildMetadata` | one image for both locales, alt "DODO Learning — Think once, in two languages." | a per-locale map: `en → /og-default.png` (alt as today) · `zh → /og-zh.png` (alt "DODO Learning · 都学书院 — 一次思考，两种语言。"); `image = ogImage ?? OG_IMAGE_DEFAULT[locale]` |
| H3 | `lib/schema.js:118–123` (`educationOrgSchema.logo`) | `og-default.png` 1200 × 630 | `/web-app-manifest-512x512.png`, 512 × 512 (the real "DO" mark; square logos are what knowledge panels and citation cards use) |
| H4 | `lib/schema.js` `articleSchema` fallback image | `og-default.png` | unchanged (now a real card) |

---

## Apply order and checks

1. **One commit for A + B + C + D** (+ E1 if B is picked): content EN/ZH, schema, llms files, footer. Bilingual parity on every row. Then the sweep the D99 lesson requires — `grep -rn "2021 she founded\|2021 年，她\|graduate degree\|研究生学位\|LocalBusiness" content lib public app` must return **only** the `/about` "2020–2025" lines and nothing else.
2. **`npm run build`** — the 14 guards. `check-cjk-coverage` only shrinks its demand here (研究生 characters leave the site) and cannot fail on these rows.
3. **F (blog) as its own commit** after the owner picks B or C — it touches routes, redirects and the sitemap and should be reviewable alone. Verify on the built output: no `/blog/undefined/` anywhere, every `_redirects` target exists in `out/`.
4. **G and H** each as their own small commit after their picks.
5. Update `docs/geo-audit-2026-09.md` § Frozen register rows F7 and F8 to "applied", `decision-index.md` statuses, and the guides' §11 D-number notes are already in place.

---

## Status · applied 2026-09-09 (afternoon)

**§ A, B, C, D, E — APPLIED** on the owner's "apply". Build green (14/14 guards), `content-audit` parity 0 · anti-dictionary 19 (baseline). Committed locally as the content commit (not pushed at the time of writing).

**§ B correction:** the post-apply sweep found three spots the table above missed — D103 is **16 rows**, not 13:

| # | File:line (post-apply) | Live (fragment) | Applied |
|---|---|---|---|
| B14 | `content/marketing.en.js:785` (`/compare` "Longitudinal knowledge") | "Every Navigator holds a graduate degree from a world top-50 university" | "…holds a degree from a world top-50 university" |
| B15 | `content/marketing.zh.js:760` (`/compare` 纵向知识) | "…的研究生学位" | "…的学位" |
| B16 | `public/llms-full.zh.txt:9` | "世界前 50 强大学的研究生学位持有者" | "世界前 50 强大学的毕业生" |

**§ F (blog) and § G (`/assessment`) — DEFERRED by the owner** until the GEO-skills pass in `docs/geo-audit-2026-09.md` § Update has been read. **§ H (OG card) — awaiting the light/dark pick.**

---

## Status · evening 2026-09-09 — "proceed as proposed"

- **§ F (blog):** elaborated in `docs/geo-audit-2026-09.md` § Update 2 → "R3 elaborated" — four options (A retire · B prune · C re-home the MCT article · **D = C plus consolidation of the two Lexile posts into `/lexile/`**, recommended) with a decision tree. New facts: none of the three posts links a source; the Lexile-score post inverts the stretch zone (states 50–100L *above* the reader's measure; MetaMetrics: 100L below to 50L above). Owner's pick pending.
- **§ G (`/assessment`):** unchanged, owner's.
- **§ H (OG card):** ruling sheet published (three compositions × two locales × five frames). A centred candidate C was added; recommendation C. Wiring H1–H4 after the pick.
- **Frozen register F1–F6, F9–F11, F13: applied** (`decision-log.md` § Frozen register). **F12 held** for a ruling.
- **Canon guard D106 caught 34 survivors on its first run** — fixed in the same change: `content/cities.js` (14 × "LCS 教学体系" → 语言循环体系; stat label), `content/faq.js` ZH (7 × 16周课程 → ELA 课程 / 16 周周期), `content/marketing.zh.js` (stat label + 2 testimonial quotes → 16 周的课程), `app/[locale]/terms/page.jsx` (EN ×3 → ELA Program; ZH ×3 十六周课程 → ELA 课程), `lib/a11y.js` (2 aria-labels), `content/zh/blog/lexile-asymmetry…mdx:65` (双语学习者 → 双语孩子, pending R3 anyway).
- **Copy-free machine layer (D108) and IndexNow (D107): shipped.**

## Status · session close 2026-09-09 — § H APPLIED (D109)

Owner ruled **white, centred** = candidate C for both locales. H1: `og-en-light-centered.png` → `public/og-default.png`, `og-zh-light-centered.png` → `public/og-zh.png`. H2: `OG_IMAGE_DEFAULT` per locale, alt strings as proposed (the EN alt now carries the first-mention pair too). H3: `Organization.logo` → `web-app-manifest-512x512.png` 512 × 512. H4: the Article fallback image follows the locale (a small extension of the proposed "unchanged"). Logged as D109. § F and § G remain the owner's — now rows A1 and A2 of `docs/ADMIN_RULINGS.md`.
